<div class="banner banner-list" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(<%= cl_image_path @list.photo.key, height: 800, crop: :fill %>);">
  <div class="container">
    <h1><%= @list.name %></h1>
  </div>
</div>
